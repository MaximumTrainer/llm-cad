{
  "permissions": {
    "allow": [
      "Bash(uv run pytest:*)",
      "Bash(uv run ruff:*)",
      "Bash(uv run mypy:*)",
      "Bash(uv run python scripts/smoke.py)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,sys,subprocess,os; d=json.load(sys.stdin); p=(d.get('tool_input') or {}).get('file_path','');  sys.exit(0) if not p.endswith('.py') else None; r=subprocess.run(['uv','run','ruff','check','--fix',p],capture_output=True,text=True); print(r.stdout or r.stderr, file=sys.stderr) if r.returncode else None\""
          }
        ]
      }
    ]
  }
}
